#!/usr/bin/env bash
set -euo pipefail

PORT="${PORT:-3000}"

exec watchexec \
  --restart \
  --watch src \
  --watch templates \
  --watch Cargo.toml \
  --watch Cargo.lock \
  --exts rs,svg,toml \
  -- "cargo run"
